// The root is intentionally lightweight. Feature UI lives behind focused
// subpath exports so a logo or theme import never loads the diff/highlight graph.
export { APP_THEME } from "./theme.config";
export { VibestudioLogo } from "./brand";
export type { VibestudioLogoProps } from "./brand";
