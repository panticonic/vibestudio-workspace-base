import { DEFAULT_THEME_CONFIG, type ThemeConfig } from "@vibestudio/shared/theme";

/** Vibestudio's shell/panel default. Games may mount a nested Theme with their own identity. */
export const APP_THEME: ThemeConfig = DEFAULT_THEME_CONFIG;
