diff --git a/dist/api/openai-codex-responses.d.ts b/dist/api/openai-codex-responses.d.ts
index 704476d..29e42f8 100644
--- a/dist/api/openai-codex-responses.d.ts
+++ b/dist/api/openai-codex-responses.d.ts
@@ -1,6 +1,11 @@
 import type { ResponseCreateParamsStreaming } from "openai/resources/responses/responses.js";
 import type { SimpleStreamOptions, StreamFunction, StreamOptions } from "../types.ts";
 export interface OpenAICodexResponsesOptions extends StreamOptions {
+    /**
+     * Renewable transport-liveness lease. Every raw WebSocket frame or SSE
+     * body chunk renews the lease; this does not limit total request time.
+     */
+    streamIdleTimeoutMs?: number;
     reasoningEffort?: "none" | "minimal" | "low" | "medium" | "high" | "xhigh" | "max";
     reasoningSummary?: "auto" | "concise" | "detailed" | "off" | "on" | null;
     serviceTier?: ResponseCreateParamsStreaming["service_tier"];
@@ -28,4 +33,5 @@ export interface OpenAICodexWebSocketDebugStats {
 export declare function getOpenAICodexWebSocketDebugStats(sessionId: string): OpenAICodexWebSocketDebugStats | undefined;
 export declare function resetOpenAICodexWebSocketDebugStats(sessionId?: string): void;
 export declare function closeOpenAICodexWebSocketSessions(sessionId?: string): void;
-//# sourceMappingURL=openai-codex-responses.d.ts.map
\ No newline at end of file
+export declare function releaseOpenAICodexWebSocketSession(sessionId?: string): void;
+//# sourceMappingURL=openai-codex-responses.d.ts.map
diff --git a/dist/api/openai-codex-responses.js b/dist/api/openai-codex-responses.js
index 705d9aa..c1d98e7 100644
--- a/dist/api/openai-codex-responses.js
+++ b/dist/api/openai-codex-responses.js
@@ -103,11 +103,11 @@ function sleep(ms, signal) {
         });
     });
 }
-function normalizeTimeoutMs(value) {
+function normalizeTimeoutMs(value, optionName = "timeoutMs") {
     if (value === undefined)
         return undefined;
     if (!Number.isFinite(value) || value < 0) {
-        throw new Error(`Invalid timeoutMs: ${String(value)}`);
+        throw new Error(`Invalid ${optionName}: ${String(value)}`);
     }
     return Math.floor(value);
 }
@@ -172,11 +172,13 @@ export const stream = (model, context, options) => {
             if (nextBody !== undefined) {
                 body = nextBody;
             }
-            const websocketRequestId = codexSessionId || uuidv7();
-            const sseHeaders = buildSSEHeaders(model.headers, options?.headers, accountId, apiKey, codexSessionId);
-            const websocketHeaders = buildWebSocketHeaders(model.headers, options?.headers, accountId, apiKey, websocketRequestId);
+            const requestId = uuidv7();
+            const transportSessionId = codexSessionId || requestId;
+            const sseHeaders = buildSSEHeaders(model.headers, options?.headers, accountId, apiKey, transportSessionId, requestId);
+            const websocketHeaders = buildWebSocketHeaders(model.headers, options?.headers, accountId, apiKey, transportSessionId, requestId);
             const bodyJson = JSON.stringify(body);
             const httpTimeoutMs = normalizeTimeoutMs(options?.timeoutMs);
+            const streamIdleTimeoutMs = normalizeTimeoutMs(options?.streamIdleTimeoutMs, "streamIdleTimeoutMs");
             const websocketConnectTimeoutMs = normalizeTimeoutMs(options?.websocketConnectTimeoutMs);
             const transport = options?.transport || "auto";
             let startEmitted = false;
@@ -197,7 +199,7 @@ export const stream = (model, context, options) => {
                                 startEmitted = true;
                                 stream.push({ type: "start", partial: output });
                             }
-                        }, httpTimeoutMs, websocketConnectTimeoutMs, cacheSessionId, grammarToolInputProperties, options);
+                        }, streamIdleTimeoutMs, websocketConnectTimeoutMs, cacheSessionId, grammarToolInputProperties, options);
                         if (options?.signal?.aborted) {
                             throw new Error("Request was aborted");
                         }
@@ -286,6 +288,19 @@ export const stream = (model, context, options) => {
                         const delayMs = retryAfterDelayMs === undefined
                             ? BASE_DELAY_MS * 2 ** attempt
                             : validateRetryDelayMs(retryAfterDelayMs, options);
+                        const retryDetails = {
+                            provider: "openai-codex",
+                            transport: "sse",
+                            phase: "sse_response_status",
+                            attempt: attempt + 1,
+                            nextAttempt: attempt + 2,
+                            maxRetries,
+                            delayMs,
+                            status: response.status,
+                            retryAfterDelayMs,
+                        };
+                        appendAssistantMessageDiagnostic(output, createAssistantMessageDiagnostic("provider_retry", new Error(`HTTP ${response.status}`), retryDetails));
+                        console.warn("[pi-ai] Retrying Codex SSE request after retryable response", retryDetails);
                         await sleep(delayMs, options?.signal);
                         continue;
                     }
@@ -309,6 +324,20 @@ export const stream = (model, context, options) => {
                         !(lastError instanceof RetryDelayExceededError) &&
                         !lastError.message.includes("usage limit")) {
                         const delayMs = BASE_DELAY_MS * 2 ** attempt;
+                        const retryDetails = {
+                            provider: "openai-codex",
+                            transport: "sse",
+                            phase: "sse_fetch",
+                            attempt: attempt + 1,
+                            nextAttempt: attempt + 2,
+                            maxRetries,
+                            delayMs,
+                        };
+                        appendAssistantMessageDiagnostic(output, createAssistantMessageDiagnostic("provider_retry", lastError, retryDetails));
+                        console.warn("[pi-ai] Retrying Codex SSE request after provider error", {
+                            ...retryDetails,
+                            error: formatThrownValue(lastError),
+                        });
                         await sleep(delayMs, options?.signal);
                         continue;
                     }
@@ -325,7 +354,7 @@ export const stream = (model, context, options) => {
                 startEmitted = true;
                 stream.push({ type: "start", partial: output });
             }
-            await processStream(response, output, stream, model, grammarToolInputProperties, options);
+            await processStream(response, output, stream, model, grammarToolInputProperties, options, streamIdleTimeoutMs);
             if (options?.signal?.aborted) {
                 throw new Error("Request was aborted");
             }
@@ -460,8 +489,8 @@ function resolveCodexWebSocketUrl(baseUrl) {
 // ============================================================================
 // Response Processing
 // ============================================================================
-async function processStream(response, output, stream, model, grammarToolInputProperties, options) {
-    await processResponsesStream(mapCodexEvents(parseSSE(response, options?.signal)), output, stream, model, {
+async function processStream(response, output, stream, model, grammarToolInputProperties, options, streamIdleTimeoutMs) {
+    await processResponsesStream(mapCodexEvents(parseSSE(response, options?.signal, streamIdleTimeoutMs)), output, stream, model, {
         serviceTier: options?.serviceTier,
         grammarToolInputProperties,
         resolveServiceTier: resolveCodexServiceTier,
@@ -545,7 +574,31 @@ function normalizeCodexStatus(status) {
 // ============================================================================
 // SSE Parsing
 // ============================================================================
-async function* parseSSE(response, signal) {
+async function readSSEChunk(reader, signal, idleTimeoutMs) {
+    if (idleTimeoutMs === undefined || idleTimeoutMs <= 0) {
+        return reader.read();
+    }
+    let timeout;
+    try {
+        return await Promise.race([
+            reader.read(),
+            new Promise((_, reject) => {
+                timeout = setTimeout(() => {
+                    const error = new Error(`SSE stream idle timeout after ${idleTimeoutMs}ms`);
+                    reject(error);
+                    void reader.cancel(error).catch(() => { });
+                }, idleTimeoutMs);
+            }),
+        ]);
+    }
+    finally {
+        if (timeout)
+            clearTimeout(timeout);
+        if (signal?.aborted)
+            void reader.cancel(signal.reason).catch(() => { });
+    }
+}
+async function* parseSSE(response, signal, idleTimeoutMs) {
     if (!response.body)
         return;
     const reader = response.body.getReader();
@@ -560,7 +613,7 @@ async function* parseSSE(response, signal) {
             if (signal?.aborted) {
                 throw new Error("Request was aborted");
             }
-            const { done, value } = await reader.read();
+            const { done, value } = await readSSEChunk(reader, signal, idleTimeoutMs);
             if (signal?.aborted) {
                 throw new Error("Request was aborted");
             }
@@ -664,7 +717,11 @@ export function closeOpenAICodexWebSocketSessions(sessionId) {
     }
     websocketSessionCache.clear();
 }
-registerSessionResourceCleanup(closeOpenAICodexWebSocketSessions);
+export function releaseOpenAICodexWebSocketSession(sessionId) {
+    closeOpenAICodexWebSocketSessions(sessionId);
+    resetOpenAICodexWebSocketDebugStats(sessionId);
+}
+registerSessionResourceCleanup(releaseOpenAICodexWebSocketSession);
 function isWebSocketSseFallbackActive(sessionId) {
     return sessionId ? websocketSseFallbackSessions.has(sessionId) : false;
 }
@@ -744,6 +801,86 @@ function closeWebSocketSilently(socket, code = 1000, reason = "done") {
     }
     catch { }
 }
+function canUseWorkersFetchWebSocketUpgrade() {
+    return typeof fetch === "function" && typeof WebSocketPair === "function";
+}
+function websocketFetchUpgradeUrl(url) {
+    const fetchUrl = new URL(url);
+    if (fetchUrl.protocol === "wss:") {
+        fetchUrl.protocol = "https:";
+    }
+    else if (fetchUrl.protocol === "ws:") {
+        fetchUrl.protocol = "http:";
+    }
+    return fetchUrl.toString();
+}
+function isConsumableWebSocket(socket) {
+    return typeof socket?.send === "function" && typeof socket?.addEventListener === "function";
+}
+function prepareVibestudioModelWebSocket(url, headers) {
+    const hook = globalThis.__vibestudioPrepareModelWebSocket;
+    if (typeof hook !== "function") {
+        return null;
+    }
+    const prepared = hook(url, headers);
+    if (!prepared || typeof prepared.url !== "string") {
+        return null;
+    }
+    return prepared;
+}
+function isWorkerdRuntime() {
+    const userAgent = globalThis.navigator?.userAgent ?? "";
+    return typeof WebSocketPair === "function" || /\b(?:Cloudflare-Workers|workerd)\b/i.test(userAgent);
+}
+function shouldReuseSessionWebSockets() {
+    const hook = globalThis.__vibestudioShouldReuseCodexWebSocket;
+    if (typeof hook === "function") {
+        return hook() !== false;
+    }
+    return !isWorkerdRuntime();
+}
+async function connectWebSocketViaFetchUpgrade(url, headers, signal, connectTimeoutMs = DEFAULT_WEBSOCKET_CONNECT_TIMEOUT_MS) {
+    if (!canUseWorkersFetchWebSocketUpgrade()) {
+        return null;
+    }
+    const wsHeaders = headersToRecord(headers);
+    wsHeaders["Upgrade"] = "websocket";
+    const controller = new AbortController();
+    let timeout;
+    const abort = () => controller.abort(signal?.reason);
+    if (signal?.aborted) {
+        abort();
+    }
+    else {
+        signal?.addEventListener("abort", abort, { once: true });
+    }
+    try {
+        timeout = setTimeout(() => {
+            controller.abort(new Error(`Codex WebSocket upgrade timed out after ${connectTimeoutMs}ms`));
+        }, connectTimeoutMs);
+        const response = await fetch(websocketFetchUpgradeUrl(url), {
+            method: "GET",
+            headers: wsHeaders,
+            signal: controller.signal,
+        });
+        const socket = response.webSocket;
+        if (!socket) {
+            throw new Error(`Codex WebSocket upgrade failed with HTTP ${response.status}`);
+        }
+        socket.accept?.();
+        if (!isConsumableWebSocket(socket)) {
+            closeWebSocketSilently(socket, 1000, "unsupported_runtime_socket_shape");
+            throw new Error("Codex WebSocket upgrade returned a pass-through WebSocket that cannot be consumed by this runtime");
+        }
+        return socket;
+    }
+    finally {
+        if (timeout) {
+            clearTimeout(timeout);
+        }
+        signal?.removeEventListener("abort", abort);
+    }
+}
 function scheduleSessionWebSocketExpiry(sessionId, entry) {
     if (entry.idleTimer) {
         clearTimeout(entry.idleTimer);
@@ -756,18 +893,29 @@ function scheduleSessionWebSocketExpiry(sessionId, entry) {
     }, SESSION_WEBSOCKET_CACHE_TTL_MS);
 }
 async function connectWebSocket(url, headers, signal, connectTimeoutMs = DEFAULT_WEBSOCKET_CONNECT_TIMEOUT_MS, env) {
+    // Workers must use fetch-upgrade so service-bound/attributed fetch routes
+    // (including Vibestudio's credential-injecting egress binding) remain in
+    // the transport path. The ambient WebSocket constructor bypasses those
+    // bindings even when it exists.
+    const fetchUpgrade = await connectWebSocketViaFetchUpgrade(url, headers, signal, connectTimeoutMs);
+    if (fetchUpgrade) {
+        return fetchUpgrade;
+    }
     const WebSocketCtor = await getWebSocketConstructor(env);
     if (!WebSocketCtor) {
         throw new Error("WebSocket transport is not available in this runtime");
     }
     const wsHeaders = headersToRecord(headers);
     delete wsHeaders["OpenAI-Beta"];
+    const vibestudioPrepared = prepareVibestudioModelWebSocket(url, headers);
     return new Promise((resolve, reject) => {
         let settled = false;
         let timeout;
         let socket;
         try {
-            socket = new WebSocketCtor(url, { headers: wsHeaders });
+            socket = vibestudioPrepared
+                ? new WebSocketCtor(vibestudioPrepared.url, vibestudioPrepared.protocols)
+                : new WebSocketCtor(url, { headers: wsHeaders });
         }
         catch (error) {
             reject(error instanceof Error ? error : new Error(String(error)));
@@ -824,7 +972,7 @@ async function connectWebSocket(url, headers, signal, connectTimeoutMs = DEFAULT
     });
 }
 async function acquireWebSocket(url, headers, sessionId, signal, connectTimeoutMs, env) {
-    if (!sessionId) {
+    if (!sessionId || !shouldReuseSessionWebSockets()) {
         const socket = await connectWebSocket(url, headers, signal, connectTimeoutMs, env);
         return {
             socket,
@@ -1144,6 +1292,10 @@ async function processWebSocketStream(url, body, headers, output, stream, model,
         }
     }
     try {
+        if (!isConsumableWebSocket(socket)) {
+            closeWebSocketSilently(socket, 1000, "unsupported_runtime_socket_shape");
+            throw new Error("Codex WebSocket upgrade returned a pass-through WebSocket that cannot be consumed by this runtime");
+        }
         socket.send(JSON.stringify({ type: "response.create", ...requestBody }));
         await processResponsesStream(startWebSocketOutputOnFirstEvent(mapCodexEvents(parseWebSocket(socket, options?.signal, idleTimeoutMs)), onStart), output, stream, model, {
             serviceTier: options?.serviceTier,
@@ -1238,18 +1390,16 @@ function buildBaseCodexHeaders(initHeaders, additionalHeaders, accountId, token)
     headers.set("User-Agent", userAgent);
     return headers;
 }
-function buildSSEHeaders(initHeaders, additionalHeaders, accountId, token, sessionId) {
+function buildSSEHeaders(initHeaders, additionalHeaders, accountId, token, sessionId, requestId) {
     const headers = buildBaseCodexHeaders(initHeaders, additionalHeaders, accountId, token);
     headers.set("OpenAI-Beta", "responses=experimental");
     headers.set("accept", "text/event-stream");
     headers.set("content-type", "application/json");
-    if (sessionId) {
-        headers.set("session-id", sessionId);
-        headers.set("x-client-request-id", sessionId);
-    }
+    headers.set("session-id", sessionId);
+    headers.set("x-client-request-id", requestId);
     return headers;
 }
-function buildWebSocketHeaders(initHeaders, additionalHeaders, accountId, token, requestId) {
+function buildWebSocketHeaders(initHeaders, additionalHeaders, accountId, token, sessionId, requestId) {
     const headers = buildBaseCodexHeaders(initHeaders, additionalHeaders, accountId, token);
     headers.delete("accept");
     headers.delete("content-type");
@@ -1257,7 +1407,7 @@ function buildWebSocketHeaders(initHeaders, additionalHeaders, accountId, token,
     headers.delete("openai-beta");
     headers.set("OpenAI-Beta", OPENAI_BETA_RESPONSES_WEBSOCKETS);
     headers.set("x-client-request-id", requestId);
-    headers.set("session-id", requestId);
+    headers.set("session-id", sessionId);
     return headers;
 }
-//# sourceMappingURL=openai-codex-responses.js.map
\ No newline at end of file
+//# sourceMappingURL=openai-codex-responses.js.map
