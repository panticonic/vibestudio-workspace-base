{
  "schemaVersion": 1,
  "service": "@workspace-extensions/templates",
  "source": "extensions/templates",
  "methods": {
    "inspect": {
      "arguments": ["{ pin } | { url, credential? }"],
      "returns": "{ pin, presentation?, repositories, files }",
      "sensitivity": "read"
    },
    "inspectAuthoring": {
      "arguments": ["{ name, description, parts }"],
      "returns": "TemplateAuthoringInspection",
      "sensitivity": "read"
    },
    "authoringParts": {
      "arguments": [],
      "returns": "{ repoPath, packageName? }[]",
      "sensitivity": "read"
    },
    "publishAuthoring": {
      "arguments": [
        "{ commandId, intent, expectedFingerprint, version, destination, credentialId?, creation? }"
      ],
      "returns": "TemplatePublication",
      "sensitivity": "write"
    }
  },
  "invariants": [
    "inspect resolves once to an exact pin and verifies a dependency-free self-contained snapshot",
    "acquired bytes remain in extension-owned workspace storage",
    "workspace creation consumes the exact inspected pin through the ordinary rootTemplate path",
    "source integration into an existing workspace uses ordinary VCS comparison and merge",
    "authoring closes workspace package and runtime dependencies and binds the plan to protected main",
    "publication does not create installed layers, runtime authority"
  ]
}
