template:
  name: Vibestudio Base
  description: Default Vibestudio system workspace.
  repositories:
    - about/about
    - about/adblock
    - about/automations
    - about/bookmarks
    - about/browser-import-inspector
    - about/collection
    - about/credentials
    - about/downloads
    - about/help
    - about/history
    - about/keyboard-shortcuts
    - about/local-models
    - about/new
    - about/permissions
    - about/server-logs
    - about/templates
    - about/testbench
    - about/workspace-history
    - apps/mobile
    - apps/remote-cli
    - apps/shell
    - apps/terminal-browser
    - extensions/browser-data
    - extensions/claude-code
    - extensions/file-tools
    - extensions/git-bridge
    - extensions/image-service
    - extensions/local-models
    - extensions/mobile-debug
    - extensions/pdf-ingest
    - extensions/react-native
    - extensions/shell
    - extensions/template-composer
    - extensions/test-runner
    - extensions/typecheck-service
    - packages/about-shared
    - packages/agent-loop
    - packages/agentic-chat
    - packages/agentic-core
    - packages/agentic-do
    - packages/agentic-protocol
    - packages/agentic-session
    - packages/cdp-client
    - packages/channel-fork
    - packages/channel-policies
    - packages/collection-orchestration
    - packages/eval
    - packages/harness
    - packages/integrations
    - packages/model-catalog
    - packages/pi-ai
    - packages/pi-core
    - packages/pubsub
    - packages/react
    - packages/runtime
    - packages/svelte
    - packages/template-composer
    - packages/template-management
    - packages/template-registry
    - packages/terminal-host-protocol
    - packages/terminal-shim
    - packages/testkit
    - packages/tool-ui
    - packages/ui
    - packages/vcs-engine
    - panels/chat
    - panels/terminal
    - projects/default
    - skills/agents
    - skills/api-integrations
    - skills/appdev
    - skills/architecture
    - skills/automations
    - skills/capabilities
    - skills/extensiondev
    - skills/gad-context
    - skills/github
    - skills/local-models
    - skills/memory
    - skills/mobile-system-testing
    - skills/onboarding
    - skills/performance
    - skills/phone-setup
    - skills/provenance-orientation
    - skills/provenance-tuning
    - skills/remote-access
    - skills/sandbox
    - skills/server-logs
    - skills/system-testing
    - skills/templates
    - skills/terminal
    - skills/vibestudio-vcs
    - skills/web-research
    - skills/workspace-dev
    - templates/default
    - templates/svelte
    - templates/vanilla
    - workers/agent-worker
    - workers/browser-data
    - workers/development
    - workers/explorer-agent
    - workers/linked-agent
    - workers/model-settings
    - workers/missions
    - workers/phone-provisioning
    - workers/pubsub-channel
    - workers/silent-agent-worker
    - workers/system-agent
    - workers/system-test-runner
    - workers/terminal-chat
    - workers/test-agent
    - workers/workspace-presentation
    - workers/testkit-driver
    - workers/workspace-source
  files:
    - .ignore
    - package.json
    - tests/helpers/ledgerTest.ts
    - types/assets.d.ts
    - tsconfig.integration.json
    - tsconfig.integration.mobile.json
    - tsconfig.json
templates:
  use: []
  registry:
    url: git+https://github.com/panticonic/vibestudio-template-registry.git
    ref: refs/heads/main
systemEpoch: 59
defaultRepo: projects/default
defaultAgentConfig:
  model: openai-codex:gpt-5.6-sol
initPanels:
  - source: panels/chat
    stateArgs:
      initialPrompt: I just opened this workspace for the first time, help me get onboarded.
      systemPrompt: >
        You are the Vibestudio onboarding assistant. Welcome new users and help
        them make Vibestudio ready for the work they actually want to do. Setup
        is durable preparation; immediately usable capabilities and ordinary
        tasks are not unfinished setup.


        ## Opening turn


        Read `skills/onboarding/SKILL.md`, give a welcome of at most two short
        paragraphs, then render `skills/onboarding/SetupHub.tsx` with
        `inline_ui` using the stable ID `onboarding-setup-overview`. Do not run
        a leading `client_eval` or pass snapshot props; the checked-in component
        loads and caches capability state itself. It contacts the optional
        template registry only after the user chooses its explained load
        action. The inline overview owns the onboarding UI; do not load or
        publish an action bar. If rendering is unavailable, give a concise
        plain-text fallback and do not invent status.


        ## Selection routing


        Inline UI choices arrive as a readable message plus a structured
        `interaction` object beside the readable user message. Never infer a
        route from the button label. For `kind: "onboarding-capability"`, use
        `client_eval` to statically import `executeOnboardingSelection` from
        `@workspace-skills/onboarding` and call it with the complete structured
        interaction object. This validates the catalog route in the inviting
        panel and performs About, panel, or shell navigation. If it returns
        `handled: false`, follow its returned owner target:

        - `owner-skill`: read the exact returned `ownerSkillPath` and use that
          capability's workflow.

        - `model-settings`: use the model-settings service and its existing
          provider/default workflow.

        - `conversation`: explain or start using the ready capability; do not
          invent setup status.


        For `kind: "onboarding-template"`, use `client_eval` to statically
        import `resolveOnboardingTemplateSelection`, call it with the complete
        interaction, then read its returned Templates skill and pass its
        registry-bound `selection` to the canonical `prepareAdd` and reviewed
        `add` workflow. The card
        does not install directly or bypass Composer review and approval.


        Unknown or retired IDs are visible errors. There is no string-matching
        fallback.

        The component handles capability checks and manual refresh itself.
        After setup, repair, reconnect, success, failure, cancellation, or
        another relevant external change, render `SetupHub.tsx` again with the
        same stable ID and no snapshot props. This bumps the card and signals it
        to refresh. Do not claim that the card now shows a particular row,
        status, or action: owner state loads asynchronously and the card is the
        authoritative observation. For an applied template operation, honor
        its `contextIntegration` before re-rendering; resolve `needs-merge`
        through ordinary VCS and never describe `unavailable` as refreshed.
        Provider secrets are collected only
        through trusted credential APIs, never in chat or inline component
        state.


        ## Presentation rules


        Composer template status and owner status reads are the source of
        the onboarding presentation. Do not enumerate an independent feature menu,
        inspect private owner storage, count credentials as proof of a live
        connection, create completion flags, or report a universal setup score.
        Optional configuration is neutral unless a selected workflow failed. A
        connection with stored credentials is connected-unverified until the
        explicit live check succeeds.

        Ask one question at a time after the overview and adapt to the user's
        intent. Use `feedback_custom` when a setup workflow must wait for user
        input, `inline_ui` for durable observations, and the compact action bar
        only for preparing, resume, reopen, or retry state. This workspace may
        run on the selected local or self-hosted server; never claim that every
        operation runs on the user's current machine.
singletonObjects:
  - source: workers/workspace-source
    className: GadWorkspaceDO
    key: workspace
  - source: workers/workspace-presentation
    className: WorkspacePresentationDO
    key: workspace-presentation
  - source: workers/model-settings
    className: ModelSettingsDO
    key: workspace-model-settings
  - source: workers/testkit-driver
    className: TestkitDriverDO
    key: workspace-testkit-driver
  - source: workers/phone-provisioning
    className: PhoneProvisioningDO
    key: workspace-phone-provisioning
  - source: workers/development
    className: DevelopmentDO
    key: workspace-development
  - source: workers/missions
    className: MissionsDO
    key: workspace-missions
services:
  - source: workers/workspace-source
    name: gad.workspace
    title: Workspace source
    action: manage semantic source, history, and workspace initialization
    description: Owns semantic source, history, projections, and workspace initialization.
    notability: everyday
    presentation:
      domain: automation
      verb: manage
    protocols:
      - vibestudio.workspace-source.v1
      - vibestudio.gad.workspace.v1
    authority:
      binding: declared
      principals:
        - host
        - user
        - code
        - session
        - mission
    durableObject:
      className: GadWorkspaceDO
  - source: workers/pubsub-channel
    name: channel
    title: Conversations
    action: send and receive messages in your conversations
    description:
      Access your workspace's conversations, including sending messages
      and reading replies.
    notability: everyday
    presentation:
      domain: sharing
      verb: act
      substanceKind: send
    protocols:
      - vibestudio.channel.v1
    authority:
      binding: declared
      principals:
        - host
        - user
        - code
        - session
        - mission
    durableObject:
      className: PubSubChannel
      context: creator
  - source: workers/browser-data
    name: browser.data
    title: Browser data
    action: use browser history, bookmarks, downloads, and preferences
    description: Store durable browser product facts outside the protected host vault.
    notability: everyday
    presentation:
      domain: web
      verb: see
    protocols:
      - vibestudio.browser-data.v1
    authority:
      binding: declared
      principals:
        - host
        - user
        - code
    durableObject:
      className: BrowserDataDO
  - source: workers/workspace-presentation
    name: workspace.presentation
    title: Workspace presentation
    action: manage panel titles, search, and launcher usage
    description: Store durable workspace presentation facts and rebuildable search indexes.
    notability: everyday
    presentation:
      domain: computer
      verb: manage
    protocols:
      - vibestudio.workspace-presentation.v1
    authority:
      binding: declared
      principals:
        - host
        - user
        - code
    durableObject:
      className: WorkspacePresentationDO
  - source: workers/model-settings
    name: models
    title: AI model settings
    action: read or change which AI models your workspace uses
    description: Read or change the AI model and provider settings for this workspace.
    notability: everyday
    presentation:
      domain: automation
      verb: manage
    protocols:
      - vibestudio.models.v1
    authority:
      binding: declared
      principals:
        - host
        - user
        - code
        - session
        - mission
    durableObject:
      className: ModelSettingsDO
  - source: workers/testkit-driver
    name: testkit-driver
    title: Test runner
    action: run workspace tests and record results
    description: Run this workspace's test suite and record results.
    notability: everyday
    presentation:
      domain: automation
      verb: act
    protocols:
      - vibestudio.testkit-driver.v1
    authority:
      binding: declared
      principals:
        - host
        - user
        - code
        - session
    durableObject:
      className: TestkitDriverDO
  - source: workers/phone-provisioning
    name: phone.provisioning
    title: Phone provisioning
    action: manage connected phones
    description: Discover, install, and pair phones through a connected desktop.
    notability: headline
    presentation:
      domain: people
      verb: manage
    protocols:
      - vibestudio.phone-provisioning.v1
    authority:
      binding: declared
      principals:
        - host
        - user
        - code
    durableObject:
      className: PhoneProvisioningDO
  - source: workers/development
    name: development
    title: Development
    action: manage development sessions
    description: Build and test exact semantic source candidates through host-owned effects.
    notability: everyday
    presentation:
      domain: automation
      verb: manage
    protocols:
      - vibestudio.development.v1
    authority:
      binding: declared
      principals:
        - host
        - user
        - code
    durableObject:
      className: DevelopmentDO
  - source: workers/missions
    name: missions
    title: Missions
    action: manage reviewed automations
    description: Author and run reviewed workspace automations.
    notability: headline
    presentation:
      domain: automation
      verb: manage
    protocols:
      - vibestudio.missions.v1
    authority:
      binding: declared
      principals:
        - host
        - user
        - code
    durableObject:
      className: MissionsDO
extensions:
  - source: extensions/git-bridge
  - source: extensions/template-composer
  - source: extensions/image-service
  - source: extensions/pdf-ingest
  - source: extensions/file-tools
  - source: extensions/typecheck-service
  - source: extensions/test-runner
  - source: extensions/browser-data
  - source: extensions/shell
  - source: extensions/claude-code
  - source: extensions/mobile-debug
  - source: extensions/react-native
  - source: extensions/local-models
apps:
  - source: apps/shell
  - source: apps/mobile
  - source: apps/remote-cli
providers:
  evalEngine:
    source: "@workspace/eval"
  evalRuntime:
    source: "@workspace/runtime"
  cdpClient:
    source: "@workspace/cdp-client"
  browserData:
    extension: extensions/browser-data
  gitInterop:
    extension: extensions/git-bridge
  claudeCode:
    extension: extensions/claude-code
trust:
  chromeApps:
    - apps/shell
    - apps/mobile
hostTargets:
  electron:
    app: apps/shell
  react-native:
    app: apps/mobile
    requiresExtensions:
      - extensions/react-native
  terminal:
    app: apps/remote-cli
git:
  remotes:
    projects:
      vibestudio:
        origin:
          url: https://github.com/panticonic/vibestudio
          branch: main
  upstreams:
    projects:
      vibestudio:
        remote: origin
        branch: main
        autoPush: false
